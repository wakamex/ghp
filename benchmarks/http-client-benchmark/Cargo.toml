[package]
name = "ghp-rust-prototype"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["http2", "json", "query", "rustls"] }
serde_json = "1"
tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread"] }
